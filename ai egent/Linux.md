# Linux

### 1. Linux的入门了解

1. Linux的诞生
   Linux由林纳斯 托瓦兹在1991年创立并发展至今成为服务器操作系统领域的核心系统

2. 什么是Linux系统的内核
   内核提供了Linux系统的主要功能，如硬件调度管理的能力
   Linux内核是免费开源的，任何人都可以查看内核的源代码，甚至是贡献源代码
3. 什么是Linux系统发行版
   内核无法被被用户直接使用，需要配合应用程序才能被用户使用
   在内核之上，封装系统级应用程序，组合在一起就称之为Linux发行版
   发行版众多，课程主要基于CentOS辅以Ubuntu进行讲解

### 虚拟机

定义：借助虚拟化技术，我们可以在系统中，通过软件：模拟计算机硬件，并给虚拟硬件安装真是的操作系统，这样，就可以在电脑中，虚拟出
一个完整的电脑，以供我们学习Linux系统。

### 虚拟机快照和远程连接

### linux的目录结构

定义：Linux的目录结构是一个树形结构
Windows系统可以拥有多个盘符，如C盘、E盘
Linux没有盘符这个概念，只有一个根目录，所有文件都在根目录下面
Linux路径的描述：

- 在Linux系统中，路径之间的层级关系，使用： / 来表示
- 在Windows系统中，路径之间的层级关系，使用: \ 来表示
-

### Linux命令入门

命令行：即Liunx终端(Terminal)，是一种命令提示符页面，以纯 "字符" 的形式操作系统，可以使用各种字符化命令对系统发出操作指令。
命令： 即Linux程序。一个命令就是一个Linux的程序，命令没有图形化页面，可以在命令行（终端中）提供字符化的反馈。

Linux命令基础格式：
HOME目录是：每一个用户在Linux系统的专属目录，默认在：/home/用户名
无论是什么命令，用于什么用途，在Linux中，命令有其通用的格式：

```md
command [-options] [parameter]
```

- command：命令本身
- -options: [可选，非必填]命令的一些选项，可以通过选项控制命令的行为细节
- paramteter: [可选非必填]命令的参数，多数用于命令指向目标等

例子：

```linux
# 意思是以列表的形式，显示 /home/itheima 目录内的内容
ls -l /home/itheima

# 意思是赋值文件夹test1 成为 test2
cp -r test1 test2
```

1. ls命令
   ls命令的作用是列出目录下的内容，语法细节如下：

```md
ls [-a -l -h] [Linux路径]
```

2. ls命令的 -a 选项
   -a 选项，表示：all的意思，即列出全部文件（包含隐藏文件）
3. ls命令的 -l 选项
   -l 选项，表示：以列表（竖向排列）的形式展示内容，并展示更多信息
4. ls命令的 -h 选项
   -h 选项，需要和-l选项搭配使用，以更加人性化的方式显示文件的大小单位
5. 命令的选项组合使用
   命令的选项是可以组合使用的，比如：ls -lah,等同于：ls -a -l -h

### 目录切换相关命令(cd/pwd)

cd 切换目录
pwd 显示当前所在目录

### 相对路径、绝对路径和特殊路径符

绝对路径：以根目录做起点，描述路径的方式，以路径/开头
相对路径：以当前目录做起点，描述路径的方式，路径不需以/开头
如无特殊需求，后续学习中，将经常使用相对路径表示
特殊路径符：

- . 表示当前目录，比如 cd ./Desktop 表示切换到当前目录下的Desktop目录内，和cd Desktop 效果一样
- .. 表示上一级目录，比如：cd .. 即可切换到上一级目录，cd ../.. 切换到上二级目录
- ~ 表示HOME目录，比如：cd ~ 即可切换到HOME目录或 cd~/Desktop,切换到HOME内的Desktio目录
- - 表示返回上一次的目录 即回退

### 创建目录命令(mkdir)

通过mkdir命令可以创建新的目录（文件夹）
mkdir来自英文：Make Directory
语法：mkdir [-p] Linux路径

- 参数必备，表示Linux路径，既要创建的文件夹路径，相对路几个或绝对路径均可
- p选项可选，表示自动创建不存在的父目录，适用于创建连续多层级的目录

### 文件操作命令part1（touch、cat、more）

1. touch创建文件
   语法： touch Linux路径
   注意：touch命令无选项，参数必填，表示要创建的文件路径

2. cat命令 查看文件内容
   语法：cat Linux文件路径

3. more命令 用于查看文件内容比较多的
   more命令同样可以查看文件内内容，同cat不同的是可以进行翻页
   语法：more Linux路径
   使用空格进行翻页，使用q退出查看

### 文件操作命令part2(cp、mv、rm)

1. cp命令赋值文件文件夹
   cp命令可以用于复制文件\文件夹，cp命令来自英文：copy
   语法：cp [-r] 参数1 参数2

- -r选项，可选，用于复制文件夹使用，表示递归
- 参数1，Linux路径，表示被赋值的文件或文件夹
- 参数2，Linux路径，表示要复制去的地方

2. mv移动文件或文件夹
   mv命令可以用于移动文件\文件夹，mv命令来自英文单词：move
   语法：mv 参数1 参数2

- 参数1，Linux路径，表示被移动的文件或文件夹
- 参数2，Linux路径，表示要移动去的地方，如果目标不存在，则进行改名，确保目标存在

3. rm删除文件、文件夹
   rm命令用于删除文件、文件夹
   rm命令来自英文单词：remove
   语法：rm [-r -f] 参数1 参数2

- -r用于删除文件
- -f用于强制删文件
- 参数1、参数2、参数N 表示要删除一个或多个文件或文件夹

通配符删除：
rm _.txt 删除所有txt文件类型的文件  
rm _ 删除当前文件夹下的所有文件
rm -rf / 或 rm -rf /\* 删除根目录下的所有文件和文件夹 谨慎使用！！！

### 查找命令(which、find)

1. which命令
   我们在前面学习的Linux命令，其实它们的本体就是一个个的二进制可执行程序，和windows系统中的.exe文件，是一个意思。
   我们可以通过which,查看所使用的一系列命令的程序文件存放在哪里
   语法：which 要查找的命令

2. find命令-按文件名查找文件
   在图形化中，我们可以方便的通过系统提供的搜索功能，搜索指定的文件
   同样，在Linux系统中，我们可以通过find命令去搜索指定的文件
   语法：find 起始路径 -name "被查找文件名"
   切换到root用户以获得管理员权限：
   sudo su ~

例子：

```python
find ./ -name "2.txt"
```

find命令 - 按文件大小查找文件
语法：find 起始路径 -size +|-[kMG]

- +、-表示大于和小于
- n表示大小数字
- kNG表示大小单位，k(小写字母)，M表示MB,G表示GB
  例子：

```md
查找小于10KB的文件：find / -size-10k
查找大于100MB的文件：find / -size+100M
查找大于1GB的文件：find / -size+1G
```

### grep、wc和管道符

### echo、tail和重定向符

### vi编辑器
