# 前端解决返回的Cookie带有Secure属性安全校验问题
前端的vite.config.ts
```ts
import { defineConfig } from "vite";
import basicSsl from "@vitejs/plugin-basic-ssl";
export default defineConfig({
    plugins:[
        basicSsl()
    ],
    server: {
      https: true
    }
})
```